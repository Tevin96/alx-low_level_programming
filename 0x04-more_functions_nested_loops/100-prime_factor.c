#include <stdio.h>
/**
 * main - a program that finds and prints the largest prime factor of the number 612852475143
 * Return: 0 (Success)
 */
int main(void)
{

long i;
long prime = 612852475143, divisor;
double square = s(number);

for (i = 1; i <= square; i++)
{
if (s % i == 0)
{
prime = s / i;
}
}
printf("%ld\n", prime);
return (0);
}
