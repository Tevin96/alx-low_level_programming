#include "main.h"
/**
 * print_buffer -  a function that prints a buffer
 * @b: buffer
 * @size: bytes
 * Return: 0 (Success)
 */
void print_buffer(char *b, int size)
{
	int i, j = 0;

	if (size <= 0)
	{
		printf("\n");
		return;
	}
	while (i < size)
	{
		j = size - 0 < 10 ? size - 0 : 10;
		printf(("%08x: ", i);
return (0);
}
